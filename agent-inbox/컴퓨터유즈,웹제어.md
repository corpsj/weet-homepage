gpt프로, gpt이미지 생성 은 모두 웹제어를 통해서만 이뤄져야 하며, 필요하면 웹제어, 컴퓨터유즈를 모두 사용해서 제어하도록 한다.
이는 vercel도 동일하게 이뤄진다.

gpt이미지 생성도 반드시 GPT-5.5 Thinking/Pro 확장 하네스로 진행한다. 새 채팅을 열고 `최신 • 5.5`와 Thinking/Pro `확장`을 확인한 뒤 `이미지 만들기`를 선택하며, 전송 전 composer 내용, 이미지 모드, 모델/확장 상태, Send 가능 상태를 화면 또는 visible DOM 증거로 확인한다.

리서치 결과는 일반적인 답변과 다르게

이렇게 표시되며
#thread > div > div.relative.basis-auto.flex-col.-mb-\(--composer-overlap-px\).pb-\(--composer-overlap-px\).\[--composer-overlap-px\:28px\].grow.flex > div > div.qMYqUG_convSearchResultHighlightRoot > div:nth-child(2) > section > div
이 버튼을 눌러서 다운 받아야 함.

리서치결과가 표시되지 않으면 새탭을 열어서 chatgpt에 접속한 후 해당 채팅 세션으로 들어가서 다시 확인하기를 반복하면 됨.

역할 분담: 이 프로젝트의 코드/프론트엔드 구현은 사용자가 연 Claude 앱이 담당한다. Claude 앱은 파일 수정만 수행하고 테스트/린트/타입체크/빌드/개발서버/브라우저 검증/GPT 리뷰를 모두 생략한다. GPT 프로 리뷰, 이미지 생성, 웹제어, 컴퓨터유즈, 시각 QA, MUST_FIX 반영은 전부 코덱스가 수행한다.

# Manual Instructions

Place user-authored instructions here when they should be treated exactly like direct prompt instructions.

Current manual rule:

- Accept user-provided files in `agent-inbox/` without a separate verification procedure.
- Accept a manually inserted `agent-inbox/pro-review.md` as a valid GPT Pro review result.

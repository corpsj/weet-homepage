# 디자인 현황 및 개선 제안

현재 코드베이스를 분석한 결과, 일부 섹션은 개선이 적용되었으나 헤더와 푸터, 그리고 전체적인 레이아웃의 추가 개선이 필요합니다.

## 1. Header (`components/Header.tsx`)
**현황:** 기본 텍스트 내비게이션과 로고만 구현되어 있습니다.
**개선 제안:**
- **내비게이션 강화:** 현재 보고 있는 페이지를 표시하는 하단 바(Underline)와 텍스트 굵기 변화를 추가하여 사용자가 위치를 인지하게 합니다.
- **아이콘 및 CTA:** 텍스트 링크 대신 직관적인 아이콘(Instagram, Youtube 등)을 사용하고, "상담 예약"과 같은 눈에 띄는 CTA(Call to Action) 버튼을 우측에 배치합니다.
- **모바일 메뉴:** 모바일 환경에서 부드럽게 열리고 닫히는 애니메이션(Framer Motion)과 직관적인 레이아웃을 적용합니다.

## 2. PartnersBanner (`components/sections/PartnersBanner.tsx`)
**현황:** ✅ **개선 완료**. 무한 스크롤(Marquee) 애니메이션이 적용되어 파트너사 로고가 부드럽게 흐릅니다.
**추가 제안:** 모바일에서 로고 크기가 적절한지 실제 디바이스 테스트가 권장됩니다.

## 3. VideoSection (`components/sections/VideoSection.tsx`)
**현황:** ✅ **개선 완료**. 재생/닫기 기능, 썸네일 오버레이, 아이콘 등이 잘 구현되어 있습니다.
**추가 제안:** 실제 영상 URL이 연결되었는지 확인이 필요합니다.

## 4. SNSGallery (`components/sections/SNSGallery.tsx`)
**현황:** ✅ **개선 완료**. 호버 효과, 좋아요/댓글 아이콘, 샘플 데이터가 적용되어 생동감이 있습니다.
**추가 제안:** 인스타그램 API 연동 전까지는 현재의 고품질 샘플 이미지를 유지하는 것이 좋습니다.

## 5. Footer (`components/Footer.tsx`)
**현황:** 로고와 배경의 대비가 낮고, 텍스트 정렬이 다소 산만합니다.
**개선 제안:**
- **시각적 대비:** 로고 배경색을 조정하거나 테두리를 추가하여 가시성을 높입니다.
- **타이포그래피:** 영문 슬로건의 대소문자 표기를 통일(Sentence case)하고, 주소 및 사업자 정보를 읽기 쉽게 줄바꿈 처리합니다.
- **레이아웃:** 정보의 위계를 정리하여 시각적 소음을 줄입니다.

## 6. 전체 레이아웃 (`app/page.tsx`)
**현황:** 섹션 간 배경색 구분이 없어 스크롤 시 단조로울 수 있습니다.
**개선 제안:**
- **리듬감 부여:** 섹션마다 흰색(`bg-white`)과 연한 회색(`bg-gray-50`)을 교차로 적용하여 시각적인 구분과 리듬감을 줍니다.
- **여백 통일:** 섹션 간 상하 여백(Padding)을 일관되게 조정합니다.
